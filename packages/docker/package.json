{
  "name": "@repo/docker",
  "private": true,
  "scripts": {
    "dev": "docker compose up -d",
    "db:reset": "docker compose rm --force --stop postgres && docker compose up -d"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
